// Geometry of the action bar rail, shared by the bar and its buttons. Everything on the bar
// is placed with absolute points: frames positioned relative to another frame hit-test at a
// different spot than they are drawn on Reforged (hover flickered, cursor state off-screen).
export const RAIL_CENTER_X = 0.4;
export const RAIL_CENTER_Y = 0.16;
export const RAIL_HEIGHT = 0.042;
export const RAIL_PADDING = 0.012;
export const BUTTON_SIZE = 0.026;
export const BUTTON_PITCH = 0.032;
